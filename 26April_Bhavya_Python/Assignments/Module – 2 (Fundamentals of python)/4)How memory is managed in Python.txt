How memory is managed in Python ?

--> The memory of python is managed like suppose if there is one variable with its value here in python stack and heap like this memory is managed so the value of the variable will be stored in the heap and the label of the variable will be stored in the stack

like this memory is managed.